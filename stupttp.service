[Unit]
Description=Stupttp HTTP Server
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/root
Environment="PORT=3000"
ExecStart=/root/stupttp
Restart=on-failure
RestartSec=5

[Install]
WantedBy=multi-user.target
